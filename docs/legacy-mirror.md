# Legacy static mirror (local)

If you have a `legacy/` folder at the repo root (e.g. old scraped static HTML), it is **listed in `.gitignore`** so it is not pushed to GitHub. It is not used by the Next.js app.
